
const initialState = [];

export default function Reducer(state = initialState, {type, payload}){

    switch (type) {
        case 'GET_TIME_TABLE':
            
            break;
    
        default:
            break;
    }
  
}